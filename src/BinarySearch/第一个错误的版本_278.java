package BinarySearch;

public class 第一个错误的版本_278 {
    public int firstBadVersion(int n) {
        int left = 1;
        int right = n;

        while (left <= right) {
            int mid = (left + right) / 2;

        }

        return 0;
    }
}
